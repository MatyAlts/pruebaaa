## 1. Baseline y herramientas

- [x] 1.1 Capturar estado git y baseline de verificaciones web disponibles (`npm run lint`, `npx tsc --noEmit` y build si el entorno permite sus requisitos); registrar ausencia de runner web o fallas preexistentes y detener los cambios afectados ante fallas, sin corregirlas dentro de este change. Verificación: resultados y limitaciones en evidencia de implementación.
- [x] 1.2 Resolver baseline estable Expo/RN/React/Node/plantilla mediante dist-tags npm y tabla oficial, cruzar Xcode requerido con inventario de imagen macOS disponible y fijar versiones elegidas e iOS mínimo. Verificación: registrar comandos, fuentes y matriz exacta como checkpoint antes del scaffold, sin usar prereleases.
- [x] 1.3 Generar proyecto mínimo TypeScript en `mobile/` con dependencias y lockfile propios; configurar identificador provisional configurable, nombre y recursos locales, sin Team ni credenciales. Verificación: `npm ci` móvil, `expo install --check`, `expo-doctor` y configuración Expo resuelta válidos; documentar scaffold/config como validación declarativa sin TDD artificial.
- [x] 1.4 Instalar runner compatible para pantalla y scripts y exponer comandos de test/lint/typecheck móviles. Verificación: runner ejecutable y baseline móvil registrado antes del primer comportamiento; configuración validada sin assertions tautológicas.

## 2. Pantalla inicial y aislamiento

- [x] 2.1 Escribir y ejecutar RED de pantalla inicial identificable, implementar mínimo y triangular primera apertura/re-render con nombre y mensaje español sin carga remota ni solicitud de cuenta. Verificación: tests de comportamiento GREEN y evidencia RED/GREEN/triangulación/refactor; render de componentes nativos con safe area y branding local.
- [x] 2.2 Excluir `mobile/**` de TypeScript/ESLint web y agregar ignores móviles para dependencias, `.expo`, nativo generado, coverage y artefactos. Verificación: checks web no procesan código nativo y baseline sigue igual o mejora exclusivamente por aislamiento; checks móviles siguen independientes. Documentar configuración declarativa y cualquier limitación preexistente.
- [x] 2.3 Validar integración JS móvil con lint, typecheck, tests y export de bundle/assets iOS Release. Verificación: comandos exitosos y recursos producidos, aclarando que export en Windows no demuestra build ni ejecución iOS.

## 3. Empaquetado y workflow macOS

- [x] 3.1 Escribir RED del empaquetado desde una `.app` de fixture; implementar script mínimo que genere IPA con una única `Payload/*.app` y preserve contenidos/permisos. Verificación: GREEN y triangulación con aplicación distinta y origen ausente/ambiguo, inspeccionando el ZIP producido y errores, con evidencia TDD.
- [x] 3.2 Escribir RED del comprobador de IPA y luego implementar validación estructural y metadata. Verificación: GREEN para fixture dispositivo con ejecutable/bundle y triangulación que rechace simulador ARM, ZIP inválido, Payload inválido, ejecutable ausente y bundle ausente/vacío; aceptar bundle Hermes binario representativo sin exigir texto y registrar evidencia TDD.
- [x] 3.3 Incorporar workflow manual con imagen macOS/Xcode/Node de baseline, instalación lockfile, checks, prebuild, pods y `xcodebuild` Release `iphoneos` sin firma ni omisión de bundling. Verificación: schema/lint de workflow y revisión de comando/destino/workspace; no requerir Secrets Apple/Expo y no usar exportación App Store. Esta tarea se valida como configuración, no con tests de existencia de YAML.
- [x] 3.4 Integrar empaquetado/comprobador y publicación condicionada de IPA y manifest de commit/herramientas; establecer timeout y retención acotados. Verificación: validación del workflow y tests de scripts confirman que fallas no producen una entrega válida; diagnósticos útiles sin datos personales.

## 4. Guía y aceptación integrada

- [x] 4.1 Escribir guía de comandos locales y Actions → descarga → firma/instalación Impactor, requisitos iOS/Developer Mode cuando aplique, Personal Team y renovación aproximada de siete días; agregar plantilla de evidencia física sin Apple ID/UDID. Verificación: guía coherente con comandos/artifact reales, distingue IPA sin firma, CI y prueba física pendiente; no afirmar capacidades futuras ni TestFlight disponibles.
- [x] 4.2 Ejecutar workflow real cuando esté disponible en GitHub y descargar/inspeccionar artefacto. Verificación: run URL y commit, logs de prebuild/pods/build, checker exitoso, IPA/manifest descargables y versiones efectivas; mantener tarea pendiente si no se ejecutó. No sustituir este resultado por export JS local.
- [x] 4.3 MANUAL: firmar e instalar en iPhone real con Impactor local y registrar primera apertura, cierre/reapertura, Metro detenido y sin internet; comprobar legibilidad/área segura. Verificación: evidencia de modelo/iOS, commit/run, bundle ID final y resultados; dejar pendiente hasta intervención/evidencia del usuario, sin declarar completo el circuito solamente porque CI pasó. Evidencia: reporte y captura de apertura más confirmación de cierre/reapertura en modo avión con Metro detenido; usuario confirma instalación del IPA generado mediante Impactor sin cambiar `com.matyalts.misaluteca`, run `35257849499`, commit `43c8333570da22632e4b38a505827bacbda6fde2`.
- [x] 4.4 Consolidar validación final y tabla TDD por tarea de comportamiento (archivo/layer/baseline/RED/GREEN/triangulación/refactor), más excepciones declarativas razonadas y limitaciones manuales. Verificación: checks locales y CLI `openspec validate initialize-ios-app --strict` ejecutados, checklist consistente con evidencia y sin tareas marcadas completas por anticipación.
