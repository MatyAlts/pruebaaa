## 1. Preparación e implementación

- [x] 1.1 Ejecutar baseline App/session/proof y leer UI web real; registrar assets/tokens y fuenteGoogle ya presente, sin editar backendauth.
- [x] 1.2 RED mínimo CTA llama login único y transita según identidad, mínimoGREEN; triangulación dobletap, restore pendiente, cancel/red/error sin sesión parcial.
- [x] 1.3 Implementar layout/login y enlaces legales con RNTL acciones reales y contraste/Dynamic Type/VoiceOver; ≥2 casos, no snapshot como único criterio.

## 2. Validación y evidencia

- [ ] 2.1 Verificar suites/types/lint/export bundled y build iOS del hito; revisar en iPhone diseño junto web, Google externo/cancel/retry y apertura sin red, dejando manual pendiente sin evidencia.

Cada comportamiento ejecutable: safety net previo; un RED mínimo ejecutado, GREEN confirmado antes de otro caso, triangulación ≥2 casos, refactor con rerun y tabla de evidencia. Declaraciones de configuración se verifican con herramientas/build; ninguna tarea física se completa sin evidencia real.
